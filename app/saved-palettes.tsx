import { Text, View } from "react-native";

export default function SavedPalletesScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
        <Text>Saved Palettes Screen</Text>
    </View>
  );
}

